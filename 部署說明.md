# 我們的記帳本 — 部署 & Google Sheet 設定說明

這個資料夾：
- `index.html` — 記帳本本體（部署這個）
- `google-apps-script.gs` — 貼到 Google Sheet 的後端程式
- `.vercelignore` — 部署時自動排除 `.gs` 和說明檔

建議順序：**先做 A（Google Sheet）拿到網址 → 再做 B（Vercel）→ 最後在網站上貼網址連線。**

---

## A. 讓資料存進你的 Google Sheet

> 靜態網站沒辦法直接寫 Google Sheet，要靠一支放在**你自己 Google 帳號**裡的小程式（Apps Script）當橋樑。免費、不用伺服器。

1. 打開你的試算表：
   <https://docs.google.com/spreadsheets/d/1JMX65lNFWWBRarcNEprHzgHqXqjC7LiXg-33hVSWeUM/edit>
2. 上方選單 **擴充功能 (Extensions) → Apps Script**
3. 把預設 `Code.gs` 內容**全部刪掉**，貼上 `google-apps-script.gs` 的**全部內容**，按 💾 存檔
   （試算表 ID 已幫你填好；若想設密碼，把檔案裡 `const TOKEN = '';` 改成 `const TOKEN = '你的密碼';`）
4. 右上角 **部署 (Deploy) → 新增部署 (New deployment)**
   - 類型（齒輪 ⚙️）：**網頁應用程式 (Web app)**
   - **Execute as（執行身分）：Me（我）**
   - **Who has access（誰可以存取）：Anyone（任何人）**
   - 按 **部署**，第一次會跳授權 → 選你的帳號 → 「進階」→「前往（不安全）」→ 允許
     （這是 Google 對自己寫的腳本的正常提醒，因為程式是你自己的）
5. 複製產生的 **Web app URL**（結尾是 `/exec`）——這就是要貼進記帳本的網址

> 之後若有改 `.gs`：部署 → 管理部署 → ✏️ 編輯 → 版本選 **New version** → 部署（網址不變）。

---

## B. 部署到 Vercel（用你的「另一個」帳號，不動現在的連接）

> Claude 這邊連著的 Vercel 是「Claude 連接器」，跟你電腦上的 **Vercel CLI 登入是兩回事**。
> 用 CLI 以另一個帳號部署，**不會**動到、也不需要解除 Claude 目前的連接。

### 方法一：Vercel CLI（最快）

在「終端機 Terminal」依序執行：

```bash
npm i -g vercel
```

```bash
cd ~/我們的記帳本
```

如果你電腦之前登入過另一個 Vercel 帳號，先登出再登入你要用的那個：

```bash
vercel logout
```

```bash
vercel login
```

（選 email 或 GitHub，登入**你要用的那個帳號**。）

然後部署：

```bash
vercel
```

- 一路 Enter 即可：Set up and deploy → 選你的帳號(scope) → 專案名稱 → 目錄用 `./` → 不用改設定
- 完成後會給一個預覽網址

正式上線：

```bash
vercel --prod
```

把最後給的 `https://xxxx.vercel.app` 打開就是你的記帳本了。
以後有改 `index.html`，再 `cd ~/我們的記帳本` 跑一次 `vercel --prod` 就更新。

### 方法二：不想用指令 → GitHub 匯入

1. 把 `~/我們的記帳本` 這個資料夾放到一個 GitHub repo
2. 到 <https://vercel.com> **登入你的另一個帳號**
3. Add New… → **Project** → Import 那個 repo → Deploy（框架選 Other，不用設定）

---

## C. 連線：把兩邊接起來

1. 打開部署好的 Vercel 網址
2. 右上 **⚙️ → 「☁️ 資料儲存」** 貼上步驟 A 拿到的 Web app URL（有設密碼就一起填 Token）→ **儲存並連線**
3. 標題下方出現 **「☁️ 已同步 Google Sheet」** 就成功了
4. 男友那邊：打開**同一個** Vercel 網址，⚙️ 填**同一個** URL → 兩人就是同一本帳

之後每筆新增/修改/刪除、預算、改名字都會自動存進 Google Sheet；打開網站時也會自動從 Sheet 載入最新資料。

> 小提醒：目前是「整份覆寫」的同步方式，若兩人**同一秒**各自修改，可能以後存的那次為準。日常分開記帳不受影響。
